// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.5.1
// - protoc             (unknown)
// source: ai/planton/copilot/copilotagent/v1/query.proto

package copilotagentv1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	CopilotAgentQueryController_GenerateChatName_FullMethodName = "/ai.planton.copilot.copilotagent.v1.CopilotAgentQueryController/generateChatName"
)

// CopilotAgentQueryControllerClient is the client API for CopilotAgentQueryController service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
type CopilotAgentQueryControllerClient interface {
	// ---------------------------------------------------------------------------
	// generateChatName
	// ---------------------------------------------------------------------------
	// PURPOSE
	//
	//	Derives a concise, user-friendly title for an existing chat session
	//	(e.g. “Terraform Module Ideas”).  The method is **purely informational**
	//	and therefore belongs on the *Query* side of the API: it does not
	//	persist data, mutate state, or emit domain events.
	//
	// REQUEST  (GenerateChatNameRequest)
	//   - string chat_id
	//     Mandatory. Stable identifier of the conversation whose stored
	//     message history will be analysed to craft the title.  The server
	//     MUST validate existence of the chat and MAY canonicalise the ID
	//     (e.g. trim, lowercase) for cache efficiency.
	//
	// RESPONSE (GenerateChatNameResponse)
	//   - string suggested_name
	//     A single-line, UTF-8 title that fits comfortably in list views.
	//     Guidelines:
	//     – ≤ 63 visible characters when possible
	//     – Avoid trailing punctuation
	//     – Preserve input language for non-English chats where feasible
	//
	// CHARACTERISTICS
	//   - Read-only & idempotent: identical chat_id → identical output when the
	//     underlying model temperature ≤ 0.2.  Safe to retry and cache.
	//   - Deterministic caching: servers MAY normalise the ID to improve hit rate.
	//   - Security: do not log PII.  Run both prompt and model output through
	//     content-filtering / redaction pipelines before storage or telemetry.
	//   - Uniqueness is *not* enforced here—handle duplicates in the command
	//     layer when the chat is actually created.
	//
	// PERFORMANCE
	//   - Typical LLM latency: 200–800 ms.  If sub-200 ms is required, cache
	//     previous results or use streaming inference with a partial update
	//     mechanism on the client.
	//
	// EXAMPLE
	//
	//	Input : chat_id = "chat-9f7c2b"
	//	Output: "Terraform AppSync Module Ideas"
	//
	// ---------------------------------------------------------------------------
	GenerateChatName(ctx context.Context, in *GenerateChatNameRequest, opts ...grpc.CallOption) (*GenerateChatNameResponse, error)
}

type copilotAgentQueryControllerClient struct {
	cc grpc.ClientConnInterface
}

func NewCopilotAgentQueryControllerClient(cc grpc.ClientConnInterface) CopilotAgentQueryControllerClient {
	return &copilotAgentQueryControllerClient{cc}
}

func (c *copilotAgentQueryControllerClient) GenerateChatName(ctx context.Context, in *GenerateChatNameRequest, opts ...grpc.CallOption) (*GenerateChatNameResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(GenerateChatNameResponse)
	err := c.cc.Invoke(ctx, CopilotAgentQueryController_GenerateChatName_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// CopilotAgentQueryControllerServer is the server API for CopilotAgentQueryController service.
// All implementations should embed UnimplementedCopilotAgentQueryControllerServer
// for forward compatibility.
type CopilotAgentQueryControllerServer interface {
	// ---------------------------------------------------------------------------
	// generateChatName
	// ---------------------------------------------------------------------------
	// PURPOSE
	//
	//	Derives a concise, user-friendly title for an existing chat session
	//	(e.g. “Terraform Module Ideas”).  The method is **purely informational**
	//	and therefore belongs on the *Query* side of the API: it does not
	//	persist data, mutate state, or emit domain events.
	//
	// REQUEST  (GenerateChatNameRequest)
	//   - string chat_id
	//     Mandatory. Stable identifier of the conversation whose stored
	//     message history will be analysed to craft the title.  The server
	//     MUST validate existence of the chat and MAY canonicalise the ID
	//     (e.g. trim, lowercase) for cache efficiency.
	//
	// RESPONSE (GenerateChatNameResponse)
	//   - string suggested_name
	//     A single-line, UTF-8 title that fits comfortably in list views.
	//     Guidelines:
	//     – ≤ 63 visible characters when possible
	//     – Avoid trailing punctuation
	//     – Preserve input language for non-English chats where feasible
	//
	// CHARACTERISTICS
	//   - Read-only & idempotent: identical chat_id → identical output when the
	//     underlying model temperature ≤ 0.2.  Safe to retry and cache.
	//   - Deterministic caching: servers MAY normalise the ID to improve hit rate.
	//   - Security: do not log PII.  Run both prompt and model output through
	//     content-filtering / redaction pipelines before storage or telemetry.
	//   - Uniqueness is *not* enforced here—handle duplicates in the command
	//     layer when the chat is actually created.
	//
	// PERFORMANCE
	//   - Typical LLM latency: 200–800 ms.  If sub-200 ms is required, cache
	//     previous results or use streaming inference with a partial update
	//     mechanism on the client.
	//
	// EXAMPLE
	//
	//	Input : chat_id = "chat-9f7c2b"
	//	Output: "Terraform AppSync Module Ideas"
	//
	// ---------------------------------------------------------------------------
	GenerateChatName(context.Context, *GenerateChatNameRequest) (*GenerateChatNameResponse, error)
}

// UnimplementedCopilotAgentQueryControllerServer should be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedCopilotAgentQueryControllerServer struct{}

func (UnimplementedCopilotAgentQueryControllerServer) GenerateChatName(context.Context, *GenerateChatNameRequest) (*GenerateChatNameResponse, error) {
	return nil, status.Errorf(codes.Unimplemented, "method GenerateChatName not implemented")
}
func (UnimplementedCopilotAgentQueryControllerServer) testEmbeddedByValue() {}

// UnsafeCopilotAgentQueryControllerServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to CopilotAgentQueryControllerServer will
// result in compilation errors.
type UnsafeCopilotAgentQueryControllerServer interface {
	mustEmbedUnimplementedCopilotAgentQueryControllerServer()
}

func RegisterCopilotAgentQueryControllerServer(s grpc.ServiceRegistrar, srv CopilotAgentQueryControllerServer) {
	// If the following call pancis, it indicates UnimplementedCopilotAgentQueryControllerServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&CopilotAgentQueryController_ServiceDesc, srv)
}

func _CopilotAgentQueryController_GenerateChatName_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(GenerateChatNameRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(CopilotAgentQueryControllerServer).GenerateChatName(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: CopilotAgentQueryController_GenerateChatName_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(CopilotAgentQueryControllerServer).GenerateChatName(ctx, req.(*GenerateChatNameRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// CopilotAgentQueryController_ServiceDesc is the grpc.ServiceDesc for CopilotAgentQueryController service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var CopilotAgentQueryController_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "ai.planton.copilot.copilotagent.v1.CopilotAgentQueryController",
	HandlerType: (*CopilotAgentQueryControllerServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "generateChatName",
			Handler:    _CopilotAgentQueryController_GenerateChatName_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "ai/planton/copilot/copilotagent/v1/query.proto",
}
