// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.5.1
// - protoc             (unknown)
// source: ai/planton/cloudops/provider/aws/v1/vpc/query.proto

package vpc

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	VpcQueryController_List_FullMethodName = "/ai.planton.cloudops.provider.aws.v1.vpc.VpcQueryController/list"
)

// VpcQueryControllerClient is the client API for VpcQueryController service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// VpcQueryController provides query operations for Amazon VPCs
// via the Planton Runner tunnel.
//
// Unlike direct AWS SDK calls from the control plane, CloudOps operations route
// requests through the tunnel to Planton Runners that have local AWS credentials.
// This enables secure, credential-free operations from customer infrastructure.
//
// Architecture flow:
// 1. Client calls CloudOps service with CloudOpsRequestContext and AWS region
// 2. CloudOps service routes through HTTP CONNECT tunnel to specified runner
// 3. Planton Runner creates EC2 client using default credentials chain
// 4. Runner calls AWS EC2 DescribeVpcs API
// 5. Response flows back through tunnel to client
//
// Credential requirements:
// The Planton Runner must have AWS credentials available via the default credential
// chain (IRSA, EC2 instance profile, environment variables, or ~/.aws/credentials).
// Required IAM permissions: ec2:DescribeVpcs
//
// All operations in this service are read-only queries. Mutating operations
// (if needed in future) would be placed in a separate CommandController.
type VpcQueryControllerClient interface {
	// List VPCs in a specified AWS region via the Planton Runner.
	//
	// This operation calls the AWS EC2 DescribeVpcs API through the runner,
	// optionally filtering by VPC IDs or tag filters. The response includes
	// metadata for each VPC including CIDR block, state, and tags.
	//
	// Common use cases:
	// - Populate VPC dropdown in cloud resource creation wizard
	// - Display VPC overview in infrastructure dashboard
	// - Filter VPCs by environment tags for environment-specific views
	//
	// Authorization:
	// Requires get permission on the runner registration identified by routing.runner_id.
	// This validates that the caller has access to the specified runner before
	// forwarding the request through the tunnel.
	//
	// Errors:
	// - INVALID_ARGUMENT: Missing or empty runner_id or region
	// - PERMISSION_DENIED: Caller lacks permission to use the specified runner
	// - UNAVAILABLE: Runner is not connected to tunnel server
	// - DEADLINE_EXCEEDED: Request timed out waiting for runner response
	// - PERMISSION_DENIED: Runner lacks AWS IAM permission for ec2:DescribeVpcs
	// - INTERNAL: Unexpected error during operation
	List(ctx context.Context, in *ListVpcsRequest, opts ...grpc.CallOption) (*ListVpcsResponse, error)
}

type vpcQueryControllerClient struct {
	cc grpc.ClientConnInterface
}

func NewVpcQueryControllerClient(cc grpc.ClientConnInterface) VpcQueryControllerClient {
	return &vpcQueryControllerClient{cc}
}

func (c *vpcQueryControllerClient) List(ctx context.Context, in *ListVpcsRequest, opts ...grpc.CallOption) (*ListVpcsResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(ListVpcsResponse)
	err := c.cc.Invoke(ctx, VpcQueryController_List_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// VpcQueryControllerServer is the server API for VpcQueryController service.
// All implementations should embed UnimplementedVpcQueryControllerServer
// for forward compatibility.
//
// VpcQueryController provides query operations for Amazon VPCs
// via the Planton Runner tunnel.
//
// Unlike direct AWS SDK calls from the control plane, CloudOps operations route
// requests through the tunnel to Planton Runners that have local AWS credentials.
// This enables secure, credential-free operations from customer infrastructure.
//
// Architecture flow:
// 1. Client calls CloudOps service with CloudOpsRequestContext and AWS region
// 2. CloudOps service routes through HTTP CONNECT tunnel to specified runner
// 3. Planton Runner creates EC2 client using default credentials chain
// 4. Runner calls AWS EC2 DescribeVpcs API
// 5. Response flows back through tunnel to client
//
// Credential requirements:
// The Planton Runner must have AWS credentials available via the default credential
// chain (IRSA, EC2 instance profile, environment variables, or ~/.aws/credentials).
// Required IAM permissions: ec2:DescribeVpcs
//
// All operations in this service are read-only queries. Mutating operations
// (if needed in future) would be placed in a separate CommandController.
type VpcQueryControllerServer interface {
	// List VPCs in a specified AWS region via the Planton Runner.
	//
	// This operation calls the AWS EC2 DescribeVpcs API through the runner,
	// optionally filtering by VPC IDs or tag filters. The response includes
	// metadata for each VPC including CIDR block, state, and tags.
	//
	// Common use cases:
	// - Populate VPC dropdown in cloud resource creation wizard
	// - Display VPC overview in infrastructure dashboard
	// - Filter VPCs by environment tags for environment-specific views
	//
	// Authorization:
	// Requires get permission on the runner registration identified by routing.runner_id.
	// This validates that the caller has access to the specified runner before
	// forwarding the request through the tunnel.
	//
	// Errors:
	// - INVALID_ARGUMENT: Missing or empty runner_id or region
	// - PERMISSION_DENIED: Caller lacks permission to use the specified runner
	// - UNAVAILABLE: Runner is not connected to tunnel server
	// - DEADLINE_EXCEEDED: Request timed out waiting for runner response
	// - PERMISSION_DENIED: Runner lacks AWS IAM permission for ec2:DescribeVpcs
	// - INTERNAL: Unexpected error during operation
	List(context.Context, *ListVpcsRequest) (*ListVpcsResponse, error)
}

// UnimplementedVpcQueryControllerServer should be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedVpcQueryControllerServer struct{}

func (UnimplementedVpcQueryControllerServer) List(context.Context, *ListVpcsRequest) (*ListVpcsResponse, error) {
	return nil, status.Errorf(codes.Unimplemented, "method List not implemented")
}
func (UnimplementedVpcQueryControllerServer) testEmbeddedByValue() {}

// UnsafeVpcQueryControllerServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to VpcQueryControllerServer will
// result in compilation errors.
type UnsafeVpcQueryControllerServer interface {
	mustEmbedUnimplementedVpcQueryControllerServer()
}

func RegisterVpcQueryControllerServer(s grpc.ServiceRegistrar, srv VpcQueryControllerServer) {
	// If the following call pancis, it indicates UnimplementedVpcQueryControllerServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&VpcQueryController_ServiceDesc, srv)
}

func _VpcQueryController_List_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(ListVpcsRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(VpcQueryControllerServer).List(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: VpcQueryController_List_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(VpcQueryControllerServer).List(ctx, req.(*ListVpcsRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// VpcQueryController_ServiceDesc is the grpc.ServiceDesc for VpcQueryController service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var VpcQueryController_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "ai.planton.cloudops.provider.aws.v1.vpc.VpcQueryController",
	HandlerType: (*VpcQueryControllerServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "list",
			Handler:    _VpcQueryController_List_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "ai/planton/cloudops/provider/aws/v1/vpc/query.proto",
}
