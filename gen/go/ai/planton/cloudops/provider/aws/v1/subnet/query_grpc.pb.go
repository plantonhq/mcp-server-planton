// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.5.1
// - protoc             (unknown)
// source: ai/planton/cloudops/provider/aws/v1/subnet/query.proto

package subnet

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	SubnetQueryController_List_FullMethodName = "/ai.planton.cloudops.provider.aws.v1.subnet.SubnetQueryController/list"
)

// SubnetQueryControllerClient is the client API for SubnetQueryController service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// SubnetQueryController provides query operations for Amazon VPC subnets
// via the Planton Runner tunnel.
//
// Unlike direct AWS SDK calls from the control plane, CloudOps operations route
// requests through the tunnel to Planton Runners that have local AWS credentials.
// This enables secure, credential-free operations from customer infrastructure.
//
// Architecture flow:
// 1. Client calls CloudOps service with CloudOpsRequestContext and AWS region
// 2. CloudOps service routes through HTTP CONNECT tunnel to specified runner
// 3. Planton Runner creates EC2 client using default credentials chain
// 4. Runner calls AWS EC2 DescribeSubnets API
// 5. Response flows back through tunnel to client
//
// Credential requirements:
// The Planton Runner must have AWS credentials available via the default credential
// chain (IRSA, EC2 instance profile, environment variables, or ~/.aws/credentials).
// Required IAM permissions: ec2:DescribeSubnets
//
// All operations in this service are read-only queries. Mutating operations
// (if needed in future) would be placed in a separate CommandController.
type SubnetQueryControllerClient interface {
	// List subnets in a specified AWS region via the Planton Runner.
	//
	// This operation calls the AWS EC2 DescribeSubnets API through the runner,
	// optionally filtering by VPC ID, subnet IDs, or tag filters. The response
	// includes metadata for each subnet including CIDR block, available IPs,
	// availability zone, and public/private classification.
	//
	// Common use cases:
	// - Populate subnet dropdown in cloud resource creation wizard (filtered by VPC)
	// - Display subnet overview in infrastructure dashboard
	// - Filter subnets by environment or tier tags
	// - Help users distinguish public vs private subnets for resource placement
	//
	// Authorization:
	// Requires get permission on the runner registration identified by routing.runner_id.
	// This validates that the caller has access to the specified runner before
	// forwarding the request through the tunnel.
	//
	// Errors:
	// - INVALID_ARGUMENT: Missing or empty runner_id or region
	// - PERMISSION_DENIED: Caller lacks permission to use the specified runner
	// - UNAVAILABLE: Runner is not connected to tunnel server
	// - DEADLINE_EXCEEDED: Request timed out waiting for runner response
	// - PERMISSION_DENIED: Runner lacks AWS IAM permission for ec2:DescribeSubnets
	// - INTERNAL: Unexpected error during operation
	List(ctx context.Context, in *ListSubnetsRequest, opts ...grpc.CallOption) (*ListSubnetsResponse, error)
}

type subnetQueryControllerClient struct {
	cc grpc.ClientConnInterface
}

func NewSubnetQueryControllerClient(cc grpc.ClientConnInterface) SubnetQueryControllerClient {
	return &subnetQueryControllerClient{cc}
}

func (c *subnetQueryControllerClient) List(ctx context.Context, in *ListSubnetsRequest, opts ...grpc.CallOption) (*ListSubnetsResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(ListSubnetsResponse)
	err := c.cc.Invoke(ctx, SubnetQueryController_List_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// SubnetQueryControllerServer is the server API for SubnetQueryController service.
// All implementations should embed UnimplementedSubnetQueryControllerServer
// for forward compatibility.
//
// SubnetQueryController provides query operations for Amazon VPC subnets
// via the Planton Runner tunnel.
//
// Unlike direct AWS SDK calls from the control plane, CloudOps operations route
// requests through the tunnel to Planton Runners that have local AWS credentials.
// This enables secure, credential-free operations from customer infrastructure.
//
// Architecture flow:
// 1. Client calls CloudOps service with CloudOpsRequestContext and AWS region
// 2. CloudOps service routes through HTTP CONNECT tunnel to specified runner
// 3. Planton Runner creates EC2 client using default credentials chain
// 4. Runner calls AWS EC2 DescribeSubnets API
// 5. Response flows back through tunnel to client
//
// Credential requirements:
// The Planton Runner must have AWS credentials available via the default credential
// chain (IRSA, EC2 instance profile, environment variables, or ~/.aws/credentials).
// Required IAM permissions: ec2:DescribeSubnets
//
// All operations in this service are read-only queries. Mutating operations
// (if needed in future) would be placed in a separate CommandController.
type SubnetQueryControllerServer interface {
	// List subnets in a specified AWS region via the Planton Runner.
	//
	// This operation calls the AWS EC2 DescribeSubnets API through the runner,
	// optionally filtering by VPC ID, subnet IDs, or tag filters. The response
	// includes metadata for each subnet including CIDR block, available IPs,
	// availability zone, and public/private classification.
	//
	// Common use cases:
	// - Populate subnet dropdown in cloud resource creation wizard (filtered by VPC)
	// - Display subnet overview in infrastructure dashboard
	// - Filter subnets by environment or tier tags
	// - Help users distinguish public vs private subnets for resource placement
	//
	// Authorization:
	// Requires get permission on the runner registration identified by routing.runner_id.
	// This validates that the caller has access to the specified runner before
	// forwarding the request through the tunnel.
	//
	// Errors:
	// - INVALID_ARGUMENT: Missing or empty runner_id or region
	// - PERMISSION_DENIED: Caller lacks permission to use the specified runner
	// - UNAVAILABLE: Runner is not connected to tunnel server
	// - DEADLINE_EXCEEDED: Request timed out waiting for runner response
	// - PERMISSION_DENIED: Runner lacks AWS IAM permission for ec2:DescribeSubnets
	// - INTERNAL: Unexpected error during operation
	List(context.Context, *ListSubnetsRequest) (*ListSubnetsResponse, error)
}

// UnimplementedSubnetQueryControllerServer should be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedSubnetQueryControllerServer struct{}

func (UnimplementedSubnetQueryControllerServer) List(context.Context, *ListSubnetsRequest) (*ListSubnetsResponse, error) {
	return nil, status.Errorf(codes.Unimplemented, "method List not implemented")
}
func (UnimplementedSubnetQueryControllerServer) testEmbeddedByValue() {}

// UnsafeSubnetQueryControllerServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to SubnetQueryControllerServer will
// result in compilation errors.
type UnsafeSubnetQueryControllerServer interface {
	mustEmbedUnimplementedSubnetQueryControllerServer()
}

func RegisterSubnetQueryControllerServer(s grpc.ServiceRegistrar, srv SubnetQueryControllerServer) {
	// If the following call pancis, it indicates UnimplementedSubnetQueryControllerServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&SubnetQueryController_ServiceDesc, srv)
}

func _SubnetQueryController_List_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(ListSubnetsRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(SubnetQueryControllerServer).List(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: SubnetQueryController_List_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(SubnetQueryControllerServer).List(ctx, req.(*ListSubnetsRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// SubnetQueryController_ServiceDesc is the grpc.ServiceDesc for SubnetQueryController service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var SubnetQueryController_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "ai.planton.cloudops.provider.aws.v1.subnet.SubnetQueryController",
	HandlerType: (*SubnetQueryControllerServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "list",
			Handler:    _SubnetQueryController_List_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "ai/planton/cloudops/provider/aws/v1/subnet/query.proto",
}
