{
  "name": "Auth0ProviderConnection",
  "kind": "Auth0ProviderConnection",
  "provider": "auth0",
  "apiVersion": "connect.planton.ai/v1",
  "description": "Auth0 tenant connection for Management API access via a Machine-to-Machine application.",
  "spec": {
    "name": "Auth0ProviderConnectionSpec",
    "fields": [
      {"name": "domain", "type": "string", "description": "Auth0 tenant domain (e.g. 'your-tenant.auth0.com').", "required": true},
      {"name": "client_id", "type": "string", "description": "Client ID for the Machine-to-Machine application.", "required": true},
      {"name": "client_secret", "type": "string", "description": "Client secret for the Machine-to-Machine application.", "required": true, "sensitive": true}
    ]
  }
}
