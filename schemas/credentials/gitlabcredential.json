{
  "name": "GitlabCredential",
  "kind": "GitlabCredential",
  "provider": "gitlab",
  "apiVersion": "connect.planton.ai/v1",
  "description": "GitLab credential for repository access and CI/CD integration via OAuth tokens.",
  "spec": {
    "name": "GitlabCredentialSpec",
    "fields": [
      {"name": "gitlab_connection_host", "type": "string", "description": "GitLab host URL (default: 'https://gitlab.com').", "required": false, "default": "https://gitlab.com"},
      {"name": "group_id", "type": "string", "description": "GitLab group name or numeric ID.", "required": true},
      {"name": "access_token", "type": "string", "description": "GitLab access token.", "required": true, "sensitive": true},
      {"name": "refresh_token", "type": "string", "description": "OAuth refresh token for obtaining new access tokens.", "required": true, "sensitive": true}
    ]
  }
}
