{
  "name": "OciCompartment",
  "kind": "OciCompartment",
  "cloudProvider": "oci",
  "apiVersion": "oci.openmcf.org/v1",
  "description": "OciCompartment is the top-level resource representing an Oracle Cloud\n Infrastructure compartment for hierarchical resource isolation.",
  "protoPackage": "org.openmcf.provider.oci.ocicompartment.v1",
  "protoFiles": {
    "api": "org/openmcf/provider/oci/ocicompartment/v1/api.proto",
    "spec": "org/openmcf/provider/oci/ocicompartment/v1/spec.proto"
  },
  "spec": {
    "name": "OciCompartmentSpec",
    "fields": [
      {
        "name": "CompartmentId",
        "jsonName": "compartmentId",
        "protoField": "compartment_id",
        "type": {
          "kind": "string"
        },
        "description": "OCID of the parent compartment where this compartment will be created.\n For top-level compartments, this should be the tenancy OCID.\n For nested compartments, this is the OCID of the parent compartment.",
        "required": true,
        "validation": {
          "required": true
        },
        "referenceKind": "OciCompartment",
        "referenceFieldPath": "status.outputs.compartmentId"
      },
      {
        "name": "Name",
        "jsonName": "name",
        "protoField": "name",
        "type": {
          "kind": "string"
        },
        "description": "Name assigned to the compartment. Must be unique across all compartments\n within the parent. This is the identifier shown in the OCI Console and\n used in IAM policy statements.\n Falls back to metadata.name if not provided.",
        "required": false
      },
      {
        "name": "Description",
        "jsonName": "description",
        "protoField": "description",
        "type": {
          "kind": "string"
        },
        "description": "Description of the compartment's purpose.\n Required by the OCI API. Use this to document what resources or teams\n the compartment is intended for.",
        "required": false,
        "validation": {
          "minLength": 1
        }
      },
      {
        "name": "EnableDelete",
        "jsonName": "enableDelete",
        "protoField": "enable_delete",
        "type": {
          "kind": "bool"
        },
        "description": "When true, the compartment will be deleted on resource destruction.\n When false (the default), the compartment is retained even after the\n IaC resource is destroyed -- this is OCI's safety mechanism to prevent\n accidental deletion of compartments containing active resources.\n Set to true only for ephemeral or development compartments.",
        "required": false
      }
    ]
  }
}
