{
  "name": "GcpDnsZone",
  "kind": "GcpDnsZone",
  "cloudProvider": "gcp",
  "apiVersion": "gcp.openmcf.org/v1",
  "description": "gcp-dns-zone",
  "protoPackage": "org.openmcf.provider.gcp.gcpdnszone.v1",
  "protoFiles": {
    "api": "org/openmcf/provider/gcp/gcpdnszone/v1/api.proto",
    "spec": "org/openmcf/provider/gcp/gcpdnszone/v1/spec.proto"
  },
  "spec": {
    "name": "GcpDnsZoneSpec",
    "fields": [
      {
        "name": "ProjectId",
        "jsonName": "projectId",
        "protoField": "project_id",
        "type": {
          "kind": "string"
        },
        "description": "The ID of the GCP project where the Managed Zone is created.\n This can be a direct value or a reference to another resource's output.\n Example: \"my-gcp-project-123\" or a reference to a GcpProject resource.",
        "required": true,
        "validation": {
          "required": true
        },
        "referenceKind": "GcpProject",
        "referenceFieldPath": "status.outputs.project_id"
      },
      {
        "name": "IamServiceAccounts",
        "jsonName": "iamServiceAccounts",
        "protoField": "iam_service_accounts",
        "type": {
          "kind": "array",
          "elementType": {
            "kind": "string"
          }
        },
        "description": "An optional list of GCP service accounts that are granted permissions to manage DNS records in the Managed Zone.\n These accounts are typically workload identities, such as those used by cert-manager,\n and are added when new environments are created or updated.",
        "required": false
      },
      {
        "name": "Records",
        "jsonName": "records",
        "protoField": "records",
        "type": {
          "kind": "array",
          "elementType": {
            "kind": "message",
            "messageType": "GcpDnsRecord"
          }
        },
        "description": "The DNS records to be added to the Managed Zone.",
        "required": false
      }
    ]
  },
  "nestedTypes": [
    {
      "name": "GcpDnsRecord",
      "description": "**GcpDnsRecord** represents a DNS record to be added to the Managed Zone.\n It includes the record type, name, values, and TTL (Time To Live) settings.",
      "protoType": "org.openmcf.provider.gcp.gcpdnszone.v1.GcpDnsRecord",
      "fields": [
        {
          "name": "RecordType",
          "jsonName": "recordType",
          "protoField": "record_type",
          "type": {
            "kind": "enum",
            "enumType": "org.openmcf.shared.networking.enums.dnsrecordtype.DnsRecordType",
            "enumValues": [
              "A",
              "AAAA",
              "ALIAS",
              "CNAME",
              "MX",
              "NS",
              "PTR",
              "SOA",
              "SRV",
              "TXT",
              "CAA"
            ]
          },
          "description": "The DNS record type (e.g., A, AAAA, CNAME).",
          "required": true,
          "validation": {
            "required": true
          }
        },
        {
          "name": "Name",
          "jsonName": "name",
          "protoField": "name",
          "type": {
            "kind": "string"
          },
          "description": "The name of the DNS record (e.g., \"example.com.\" or \"dev.example.com.\").\n This value should always end with a dot to signify a fully qualified domain name.",
          "required": true,
          "validation": {
            "required": true
          }
        },
        {
          "name": "Values",
          "jsonName": "values",
          "protoField": "values",
          "type": {
            "kind": "array",
            "elementType": {
              "kind": "string"
            }
          },
          "description": "The list of values for the DNS record.\n If the record type is CNAME, each value in the list should end with a dot.",
          "required": false,
          "validation": {
            "minItems": 1
          }
        },
        {
          "name": "TtlSeconds",
          "jsonName": "ttlSeconds",
          "protoField": "ttl_seconds",
          "type": {
            "kind": "int32"
          },
          "description": "The Time To Live (TTL) for the DNS record, in seconds.",
          "required": false,
          "default": "60",
          "oneofGroup": "_ttl_seconds"
        }
      ]
    }
  ]
}
